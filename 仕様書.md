# サプリンク 仕様書

> アプリ名: **サプリンク** / 物品オーダーシステム

最終更新: 2026-05-08

## 1. 背景・目的

病院の手術室（OP室）看護師がサプライ課へ手術用物品を依頼するための Web アプリ。

### 1.1 現状の課題

- 依頼手段が電話のみで、物品名称が聞き取りづらい
- 看護師が物品の正式名称を把握しておらず、通称で依頼するため、サプライ課の新人スタッフが物品を正確に認識できない
- 急な依頼への電話対応がサプライ課新人スタッフの精神的負担になっている

### 1.2 解決ゴール

両者間で物品を正確に認識し、確実に依頼・受付・配送できるオーダーシステムを構築する。スマートフォンでも操作できることを重視。

## 2. 技術スタック・インフラ

| 項目 | 内容 |
|---|---|
| フレームワーク | Next.js 15 (App Router) + TypeScript + React 19 |
| UI | Tailwind CSS + lucide-react（モノクローム系アイコン） |
| データベース | **Supabase Postgres** |
| リアルタイム | **Supabase Realtime**（postgres_changes 購読） |
| ファイル | **Supabase Storage**（item-photos バケット） |
| デプロイ | Vercel (Production / Preview 自動デプロイ) |
| 想定端末 | スマートフォン中心（PC でも操作可能） |
| 通知 | WebAudio API（依頼受信時のアラーム音） |
| 認証 | **未導入**（PBL/デモ用途。URL秘匿運用） |

## 3. 利用者と利用環境

| ロール | 利用端末 | 主な画面 |
|---|---|---|
| 手術室看護師（依頼側） | iPhone 等のスマートフォン | 検索 → カート → 送信 |
| サプライ課スタッフ（受付側） | PC / タブレット | 受付状況、依頼詳細、ピッキング |
| 物品マスタ管理者 | PC | 設定 → 物品マスタ |

> 認証・ロール分離は未実装。URL を知っていれば誰でも全機能を利用可能。

## 4. 画面構成

| # | 画面 | パス | 種別 | 役割 |
|---|---|---|---|---|
| 1 | 物品検索 | `/` | ページ | カテゴリ絞込・物品検索・カート追加 |
| 2 | カート / 配送依頼モーダル | `/` 上のオーバーレイ | モーダル | 依頼確認・手術室選択・送信 |
| 3 | 受付状況 | `/status` | ページ | 当日依頼の進捗カード（依頼中／ピッキング中／配送済） |
| 4 | 依頼詳細 | `/status/[id]` | ページ | 内容確認・ピッキングチェック・状態遷移・削除 |
| 5 | 依頼履歴 | `/status/history` | ページ | 全期間の履歴・フィルタ・一括削除 |
| 6 | 設定 | `/settings` | ページ | 物品マスタ取込・写真登録 |
| 7 | 写真ライトボックス | `/` 上のオーバーレイ | モーダル | 検索カードの写真クリックで全画面拡大 |

## 5. 機能要件

### 5.1 物品検索（画面 1）

- アプリ起動直後に表示。手術室選択ゲートは無し。
- 検索窓でリアルタイム絞込（入力と同時に反映、検索ボタン無し）。
- 検索対象: 材料名／製品記号／棚番号／メモ／物品コード。
- 文字種マッチング（あいまい検索）:
  - 大文字↔小文字、ひらがな↔カタカナ、半角カナ↔全角カナ、全角英数字↔半角英数字
  - 半角スペース区切りで AND 検索
- 検索窓右側に **カテゴリ絞込プルダウン**（マスタにカテゴリが登録されているときのみ表示）。
- 物品はカード形式で一覧表示:
  - 写真サムネ（64×64、固定サイズ）
  - カテゴリ（小さい大文字、写真と名前の上）
  - 材料名（太字）／製品記号
  - メモ（左）／**棚番号（右下、太字で強調）**
  - **写真をタップで全画面ライトボックス**（ピンチで拡大確認）
- カート在中の物品は強調表示:
  - 枠線・背景色を強調、左端にアクセントバー
  - 右上を「✓ カート N」（黒地白文字）に置換
- 物品カードのテキスト部分タップで **数量入力ポップアップ** 表示。

### 5.2 カート / 依頼フロー（画面 2 = モーダル）

- カートは画面右下のフロート FAB から開く（`/status*` 配下では非表示）。
- カートはスマホでもPCでも **画面中央** に表示（ボトムシートではない）。
- カート操作:
  - ± / 直接入力で数量変更
  - 0 または空で削除（カートから外れる）
  - 「すべて削除」でカートクリア
- **手術室の選択（必須）**:
  - カート内の **必須プルダウン**（OP1〜OP10）。
  - 未選択時は赤枠＋赤リングで警告、送信ボタン disabled。
  - 一度選択した部屋は `sessionStorage` に保存され、次回カート開いた時に preselect。
- 「配送依頼を送信」で確定。送信成功時はモーダル内で「依頼を送信しました」表示 → 「続けて依頼する」で閉じる。
- 数量入力ポップアップ:
  - 半角数字のみ受付（IME 入力された全角は自動除去）
  - フォーカス時に全選択、Enter で確定、Esc でキャンセル
  - すでにカートに在中なら「カートを更新」（**上書き**、加算しない）に表示切替
  - 「カートから削除」ボタンも表示（誤操作回避のため検索カード側には置かない）
- カート内容は `sessionStorage` に保持（リロード後も維持、別ブラウザでは共有されない）。

### 5.3 ステータス管理（画面 3 / 画面 4）

#### ステータス遷移（3 段階）

```
依頼中 (requested) → ピッキング中 (picking) → 配送済 (delivered)
```

- サーバ側で遷移を検証（逆遷移不可、`requested → delivered` の1段飛ばしのみ管理用に許可）。
- 各遷移で `pickedAt` / `deliveredAt` を自動記録。

#### 受付状況（画面 3）

- 表示範囲は **当日分のみ**:
  - 依頼中・ピッキング中: 当日全件
  - 配送済: 当日分の最新10件まで（10件超なら「履歴で確認」リンク）
- カラー区分: 依頼中（黒バッジ）／ピッキング中（アンバー）／配送済（グレー）
- カードに含まれる情報: 手術室・状態・受付時刻・物品 N 種 / 合計M個・先頭3件のプレビュー
- カード自体は操作ボタン無し → タップで詳細画面へ。
- Realtime 接続表示（接続中／接続待ち）と通知音 ON/OFF トグル。
- **通知音とアラーム動作**:
  - 通知音 ON 時、新規依頼受信で WebAudio による 4 音アラーム（sine + square + 倍音をミックスした注意喚起音、~1.6秒）を鳴らす。
  - **「依頼中」が残っている間は 15 秒ごとに自動再鳴動**（誰かがピッキング開始 or 配送済にすると停止）。
  - 通知音 ON にした瞬間に依頼中があれば即座に1回鳴らす（状態を耳で確認できる）。
  - `localStorage` に ON/OFF 状態を永続化、リロード後の最初のユーザ操作で AudioContext 自動再開。
  - アラーム再生中は画面上に「アラーム中（依頼中 N 件）」のパルスバッジを表示。
- **Wake Lock**: 通知音 ON の間は **Screen Wake Lock API でスクリーンスリープを防止**（受付PCのモニタ／スマホ画面が消えなくなる）。タブが非表示になると一旦解放、再表示で自動再取得。
- **制約 / 限界**:
  - **PC やスマホが完全にスリープした状態では音が鳴りません**（ブラウザ仕様）。
  - スリープ中も通知したい場合は Web Push + Service Worker + プッシュ配信サーバ（Vercel Edge Functions / Supabase Realtime + push 等）の追加実装が必要。次フェーズの拡張候補。
  - iOS Safari は PWA として「ホーム画面に追加」した状態 + iOS 16.4 以降で push 通知に対応。Android は Chrome がより寛容。

#### 依頼詳細・ピッキング（画面 4）

- ヘッダに手術室・状態・受付/ピッキング開始/配送完了 のタイムスタンプ。
- 物品は **縦並びカード**（モバイル向け）:
  - 写真サムネ・材料名・製品記号・棚番号（太字）・コード・メモ・数量
  - **ピッキング中** のときのみ各カード下部に大きな **「ピッキング完了」チェックボタン**（44px+ タップターゲット）
  - チェック済み: 緑背景＋取り消し線＋写真半透明
- ヘッダに進捗バッジ「チェック X / Y」表示。
- ピッキングチェック状態は **`localStorage` に依頼ID単位で保存**（リロードでも維持、配送完了時に自動クリア）。
- 状態遷移ボタン（画面下部、スマホでは固定スティッキー）:
  - 依頼中 → 「ピッキング開始」
  - ピッキング中 → **「配送完了」**（全物品チェック済みでないと disabled）
  - 配送済 → ボタンなし
- **配送完了後は自動で `/status` に戻る**（受付タブの「配送済」セクションに表示される）。
- 「依頼を削除」ボタン: **任意の状態で削除可能**（確認ダイアログ付き、配送済は警告文増）。

### 5.4 依頼履歴（画面 5）

- 全期間の依頼を **カードグリッド**（スマホ1列／タブレット2列／PC 3列）で表示。
- フィルタ:
  - 日付（カレンダー、当日のみ表示）
  - 状態（依頼中／ピッキング中／配送済）
  - 手術室
- 各カード: 手術室・状態・受付/ピッキング/配送タイムスタンプ・全行リスト・合計数量。
- **削除モード**:
  - 右上「削除モード」ボタンで切替
  - 各カードがチェックボックス付き選択 UI に変化
  - 画面下部の固定アクションバー: 全選択／選択解除／選択した依頼を削除
  - 並列 DELETE で一括処理、即時 UI 反映
  - 確認ダイアログ付き

### 5.5 物品マスタ管理（画面 6 = 設定 → 物品マスタ）

- 設定画面右上の歯車アイコンから遷移。
- 3 つのセクションで構成。

#### インポート

- **CSV / TSV / Excel(.xlsx, .xls)** 対応（SheetJS 利用）
- 列見出し（別名対応）:
  - 物品コード／コード／code → `code`
  - 材料名／品名／name → `name`
  - 製品番号／製品記号／規格／spec → `spec`
  - 棚番／棚番号／棚／shelf → `shelf`
  - メモ／備考／memo → `memo`
  - カテゴリ／カテゴリー／分類 → `category`
- インポート前にプレビュー＋警告（コード重複・材料名空など）。
- 「この内容で更新」で **Supabase items テーブル全置換**（DELETE → INSERT）。
- **写真の関連付けは保持**: 既存の `photo_path` を引き継いで再投入する（同じコードの物品の写真は CSV 再投入で消えない）。
- 「マスタを空にする」で全削除（確認ダイアログ付き）。
- Supabase Realtime で他端末にも即時反映。

#### 写真の一括取り込み

- ファイル名先頭の数字を物品コードとして自動マッチ:
  - `100.jpg` / `100.png` → コード 100
  - `100_label.jpg` / `IMG_100.jpg` → 先頭が数字のものはコード 100
- 複数ファイル選択 / フォルダドラッグ&ドロップ対応。
- 結果テーブルで「成功 / 未マッチ / エラー」を可視化。
- 各画像は **約80KB以下に自動圧縮**（後述）。

#### 物品一覧

- カード形式で一覧表示（カテゴリ＋名前＋仕様＋棚（太字）＋コード＋メモ）。
- 検索フィルタ・カテゴリフィルタ。
- 行をタップで展開し、**写真の登録 / 撮影 / 削除** を実行:
  - ファイル選択（ドラッグ&ドロップ可）
  - **カメラで撮影**（モバイル: `<input type="file" capture="environment">` で背面カメラ起動）
  - 削除（確認ダイアログ）

### 5.6 写真の自動圧縮

- iPhone 写真の数MB級にも対応。**目標サイズ80KB以下** に自動圧縮。
- 段階的なリトライ:
  - 長辺と JPEG 品質の組合せを高品質→低品質の順に試行
  - 1024×0.7 → 800×0.7 → 800×0.6 → 640×0.65 → ... → 400×0.45 まで
  - 最初に80KB以下を達成した結果を採用、未達成なら最も小さい結果
- 結果: 2MB の元画像 → 約50KB に圧縮（実測例）。
- canvas 経由のため EXIF メタデータ（GPS 等）も実質除去される。

### 5.7 写真の表示

- 物品が `photo_path` を持つ場合、Supabase Storage の公開 URL を組み立てて img タグに直接渡す。
- URL に `?v=<items.updated_at>` をクエリ付加してキャッシュバスター。
- 写真未登録は ImageOff アイコンのプレースホルダ。
- 検索画面・数量ダイアログ・カート・受付詳細・履歴・設定すべてで同じサムネ表示。
- **検索画面のサムネをタップで全画面ライトボックス**（背景クリック / ✕ / Esc で閉じる）。

### 5.8 依頼ラインのスナップショット保存

- 依頼送信時、各物品行には **物品マスタの当時の値が snapshot として固定保存** される:
  - `name`, `spec`, `shelf`, `memo`, `category`
- 表示時は `snapshot → 現在のマスタ → 「#コード」のみ` の優先順位でフォールバック。
- **物品マスタを変更しても過去履歴の表示は依頼時の状態が保たれる**。
- 写真は Storage の同一キーを上書きする運用のため snapshot に含めず、表示時は items.photo_path を参照（写真が消えた場合は写真なし表示）。

## 6. UI / デザイン方針

- **モバイルファースト**。スマホでの操作を中心に設計。
- 事務用途のためシンプルで視認性の高い構成を優先。
- アイコンは **モノクローム系**（lucide-react）。色付きアイコンは使わない。
- 配色はモノトーン基調＋状態色（アンバー＝ピッキング中、エメラルド＝完了）。
- フォーム要素は **タップターゲット 44px 以上** を目安に確保。
- スマホでの主要操作ボタンは画面下部に **固定スティッキー配置**（ピッキング完了・削除モードの一括削除など）。
- テーブル形式は撤廃し、すべて **カードレイアウト**（横スクロール無し）。

## 7. データモデル

### 7.1 物品マスタ（`items` テーブル / TypeScript `Item`）

```ts
type Item = {
  code: number;            // 物品コード（PK）
  name: string;            // 材料名
  spec: string;            // 製品記号
  shelf: string;           // 棚番号
  memo: string;            // 通称・検索しやすいワード
  category?: string;       // カテゴリ（任意）
  photoPath?: string;      // Storage 内のオブジェクトキー（例: "100.jpg"）
  updatedAt?: string;      // items.updated_at （写真URLのキャッシュバスタ）
};
```

DB スキーマ:

```sql
CREATE TABLE items (
  code        INTEGER PRIMARY KEY,
  name        TEXT NOT NULL,
  spec        TEXT NOT NULL DEFAULT '',
  shelf       TEXT NOT NULL DEFAULT '',
  memo        TEXT NOT NULL DEFAULT '',
  category    TEXT,
  photo_path  TEXT,
  updated_at  TIMESTAMPTZ NOT NULL DEFAULT NOW()
);
-- updated_at 自動更新トリガ + supabase_realtime に追加済
```

### 7.2 依頼（`orders` テーブル / TypeScript `Order`）

```ts
type CartLine = {
  itemCode: number;
  quantity: number;
};

type OrderLineSnapshot = {
  name: string;
  spec: string;
  shelf: string;
  memo: string;
  category?: string;
};

type OrderLine = CartLine & {
  snapshot?: OrderLineSnapshot;
};

type OrderStatus = "requested" | "picking" | "delivered";

type Order = {
  id: string;                 // UUID
  room: string;               // OP1..OP10
  lines: OrderLine[];         // JSONB（各行に snapshot 同梱）
  createdAt: string;          // ISO8601
  status: OrderStatus;
  pickedAt?: string;
  deliveredAt?: string;
  completedAt?: string;       // 旧仕様互換のためのみ保持（読み込み時に deliveredAt にマージ）
};
```

DB スキーマ:

```sql
CREATE TABLE orders (
  id            UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  room          TEXT NOT NULL,
  lines         JSONB NOT NULL,
  status        TEXT NOT NULL DEFAULT 'requested'
                CHECK (status IN ('requested','picking','delivered')),
  created_at    TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  picked_at     TIMESTAMPTZ,
  delivered_at  TIMESTAMPTZ
);
-- supabase_realtime に追加済
-- インデックス: idx_orders_created_at (DESC), idx_orders_status
```

### 7.3 写真（Supabase Storage）

- バケット: `item-photos`（Public bucket）
- パス: 物品コード `.jpg` 固定（例: `100.jpg`）
- 公開 URL でアプリから img タグ直叩き

## 8. API

| メソッド | パス | 役割 |
|---|---|---|
| GET | `/api/items` | 物品マスタ取得（Supabase 経由、空ならバンドルされた fallback） |
| POST | `/api/items` | 物品マスタ全置換（既存 photo_path は保持） |
| GET | `/api/orders` | 依頼一覧（新しい順） |
| POST | `/api/orders` | 依頼作成（`{ room, lines: [{itemCode, quantity, snapshot?}] }`） |
| GET | `/api/orders/{id}` | 依頼詳細 |
| PATCH | `/api/orders/{id}` | 状態遷移（`{ status: "picking" or "delivered" }`） |
| DELETE | `/api/orders/{id}` | 依頼削除（任意の状態） |

## 9. クライアント側のローカル保存

| 用途 | 保存先 | キー |
|---|---|---|
| カート内容 | sessionStorage | `or-cart` |
| 手術室の最終選択 | sessionStorage | `or-room` |
| 通知音 ON/OFF | localStorage | `or-supply-audio-on` |
| ピッキングチェック状態（依頼ID別） | localStorage | `or-supply-picking-checks-<id>` |

物品マスタ・写真・履歴は **すべて Supabase 上の真実のソース**。端末ローカルキャッシュは持たない。

## 10. 主要ファイル

### サーバ・ライブラリ

| ファイル | 役割 |
|---|---|
| `lib/supabaseBrowser.ts` | ブラウザ用 Supabase クライアント（singleton） |
| `lib/supabaseServer.ts` | サーバ用 Supabase クライアント |
| `lib/db.ts` | 依頼の Supabase CRUD (`orderStore`) |
| `lib/itemsDb.ts` | 物品マスタの Supabase CRUD（`listItems` / `replaceAllItems`） |
| `lib/items.ts` | バンドルされた既定物品（Supabase 空時の fallback） |
| `lib/types.ts` | 型定義＋ `lineDisplayItem` ヘルパー＋ `nextOrderStatus` |
| `lib/photoStore.ts` | 写真の Storage 書込 / 公開 URL 組立 / 圧縮 |
| `lib/useItems.ts` | items 取得 + Realtime 購読フック |
| `lib/normalize.ts` | 検索文字列正規化（NFKC + lowercase + ひら→カナ） |
| `lib/beep.ts` | WebAudio アラーム |
| `lib/pickingChecksStore.ts` | ピッキングチェック状態の localStorage |

### ページ

| ファイル | 役割 |
|---|---|
| `app/page.tsx` | 物品検索画面（手術室ゲートなし） |
| `app/status/page.tsx` | 受付状況一覧（当日分） |
| `app/status/[id]/page.tsx` | 依頼詳細 |
| `app/status/history/page.tsx` | 履歴ページ |
| `app/settings/page.tsx` | 設定（物品マスタタブ） |
| `app/api/**` | API ルート群 |

### コンポーネント

| ファイル | 役割 |
|---|---|
| `components/Header.tsx` | グローバルヘッダ（検索/受付/履歴/設定） |
| `components/SearchClient.tsx` | 物品検索 UI（カテゴリ絞込・写真ライトボックス） |
| `components/CartModal.tsx` | カート / 配送依頼モーダル（手術室必須プルダウン） |
| `components/QuantityDialog.tsx` | 数量入力ポップアップ |
| `components/FloatingCartButton.tsx` | 右下フロート FAB |
| `components/StatusClient.tsx` | 受付状況 UI（Realtime 購読） |
| `components/DetailClient.tsx` | 依頼詳細 UI（ピッキングチェック・状態遷移・削除） |
| `components/HistoryClient.tsx` | 履歴 UI（Realtime 購読・一括削除モード） |
| `components/SettingsClient.tsx` | 設定タブナビ |
| `components/ItemMasterTab.tsx` | 物品マスタ管理（CSV/Excel 取込・行展開で写真編集） |
| `components/ItemPhotoEditor.tsx` | 単発写真アップロード／カメラ撮影／削除 |
| `components/ItemPhotoThumb.tsx` | 写真サムネ表示 |
| `components/PhotoBatchImport.tsx` | 写真の一括取り込み |
| `components/PhotoLightbox.tsx` | 写真の全画面拡大表示 |
| `components/providers.tsx` | RoomContext / CartContext |

## 11. セキュリティ・運用上の注意

### 現状（PBL/デモ前提）

| 項目 | 状態 |
|---|---|
| 認証 | **未導入**（誰でも全機能利用可能） |
| RLS（Row Level Security） | **無効**（anon キーで全テーブル CRUD 可能） |
| Storage バケット | Public、anon write/update/delete 許可 |
| URL の秘匿運用 | Vercel Preview / Production URL を限定共有することで担保 |

### 本番運用前に対処すべき項目

| Step | やること | なぜやるのか | 想定工数 |
|---|---|---|---|
| 1 | Supabase Auth（メール認証）を導入 | URL を知っているだけで誰でも全操作できる現状を解消する | 半日〜1日 |
| 2 | RLS を有効化し authenticated ロールのみ許可 | anon キーを使った直接 API 叩きを防ぐ | 数時間 |
| 3 | Storage を authenticated 書き込みに変更 | 大量アップロードによる無料枠枯渇を防ぐ | 30分 |
| 4 | 役割分離（admin / supply / operator）と RLS 細分化 | 物品マスタ全削除や他部屋依頼削除などの誤操作・内部不正を防ぐ | 1日 |
| 5 | 監査ログ + 定期バックアップ | 破壊操作の追跡・復旧。無料プランは7日自動バックアップのみ | 半日 |
| 6 | Vercel Middleware でレート制限 | DoS および無料枠 API コール枯渇を防ぐ | 数時間 |

### Supabase Advisor で出ている警告

- **RLS Disabled in Public**（orders / items, CRITICAL）→ Step 1+2 で対処
- **Public Bucket Allows Listing**（item-photos）→ 個別 SELECT ポリシーを撤去で対処済 / 残っている場合は対処
- **Function Search Path Mutable**（set_updated_at）→ `SET search_path = ''` で対処
- **Unused Index**（orders）→ 性能のみ、放置可

## 12. 補足・今後の拡張

- 物品マスタは CSV/Excel 取込で運用。GUI 上での個別編集（追加・更新）は未実装（必要なら別途）。
- 通知強化（OS Push、メール、デスクトップ通知）は未着手。
- 院内システム（電子カルテ等）連携は未着手。連携時は PHI 取扱に応じた追加要件が発生。
- 履歴の長期運用（年単位）に備えるなら定期エクスポート＋古いデータの削除運用を検討。

## 13. 変更履歴

| 日付 | 変更内容 |
|---|---|
| 2026-05-06 | 初版作成 |
| 2026-05-06 | カート / 配送依頼を独立ページからモーダル化、フロート FAB 追加、アプリ名「サプリンク」決定、検索カードのカート在中強調、数量上書き仕様、通知音永続化など多数 |
| 2026-05-07 | 履歴ページ（端末ローカル）追加、ロゴ画像、設定ページ・物品マスタ取込 / 写真機能追加、検索カテゴリ絞込、ステータスを3段階（依頼中／ピッキング中／配送済）に再設計 |
| 2026-05-07 | Supabase 移行 — 依頼を Postgres + Realtime に、物品マスタを共有 DB に、写真を Storage に、履歴を Supabase クエリに移行（4 Phase） |
| 2026-05-08 | スマホ最適化（カードレイアウト全面化、固定スティッキー操作バー）、ピッキングチェックリスト機能、配送完了後の自動遷移 |
| 2026-05-08 | カート手術室を必須プルダウン化、トップページの選択ゲートを撤去、検索カードのレイアウト整理 |
| 2026-05-08 | 依頼ラインのスナップショット保存（マスタ変更で過去履歴が変わらないように） |
| 2026-05-08 | 履歴の一括削除モード追加、配送済も削除可能に |
| 2026-05-08 | アラーム強化: 音量増・耳につく音色に変更、「依頼中」が残っている間 15 秒ごとにループ、Wake Lock でスクリーンスリープ防止 |
