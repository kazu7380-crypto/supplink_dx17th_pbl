# サプリンク 仕様書

> アプリ名: **サプリンク** / サブタイトル: **物品オーダーシステム**

最終更新: 2026-05-06

## 1. 背景・目的

病院の手術室（OP室）看護師がサプライ課へ手術用物品を依頼するための Web アプリを構築する。

### 1.1 現状の課題

- 依頼手段が電話のみで、物品名称が聞き取りづらい
- 看護師が物品の正式名称を把握しておらず、通称で依頼するため、サプライ課の新人スタッフが物品を正確に認識できない
- 急な依頼への電話対応がサプライ課新人スタッフの精神的負担になっている

### 1.2 解決ゴール

両者間で物品を正確に認識し、確実に依頼・受付できるオーダーシステムを構築する。

## 2. 技術スタック・インフラ

| 項目 | 内容 |
|------|------|
| フレームワーク | Next.js 15 (App Router) + TypeScript + React 19 |
| UI | Tailwind CSS + lucide-react（モノクローム系アイコン） |
| 動作環境 | Web ブラウザ（PC） |
| デプロイ | Vercel |
| 通知 | Server-Sent Events (SSE) + WebAudio API |
| データベース | 未確定。**共有サーバ上の Excel ファイル** を使用する方向で検討中（2026-05-06 時点）。現状は `lib/db.ts` のインメモリストア |

## 3. 利用者と利用環境

| ロール | 利用 PC | 主な画面 |
|------|------|------|
| 手術室看護師（依頼側） | 各手術室の PC | 物品検索（カートモーダル含む） |
| サプライ課スタッフ（受付側） | サプライ課の PC | 受付状況、依頼詳細 |

## 4. 画面構成

| # | 画面 | パス | 種別 | 役割 |
|---|------|------|------|------|
| 1 | 物品検索画面 | `/` | ページ | 物品の検索・カート追加 |
| 2 | カート / 配送依頼モーダル | `/` 上のオーバーレイ | モーダル | 依頼の確認・数量変更・送信 |
| 3 | 受付状況画面 | `/status` | ページ | 依頼一覧（カード形式） |
| 4 | 依頼詳細画面 | `/status/[id]` | ページ | 依頼の詳細情報・完了処理 |

> **画面 2 はモーダル化（変更点）**
>
> 当初は独立ページ `/cart` を想定していたが、検索 → カート確認 → 送信という一連の動作で画面遷移を発生させない方が UX 上望ましいため、画面 1 のモーダルとして表示する仕様に変更。送信完了画面もモーダル内で完結させる。

## 5. 機能要件

### 5.1 物品検索（画面 1）

- 配送依頼したい物品をリアルタイムに絞り込んで検索できる。
- 検索対象フィールド：**正式名称（材料名）／製品記号／棚番号／メモ（通称）／物品コード**。
- **検索ボタンは設けない**。文字入力と同時に自動で絞り込みを行う。
- 文字種マッチングは以下を区別しない（あいまい検索）：
  - 大文字 ↔ 小文字
  - ひらがな ↔ カタカナ
  - 半角カナ ↔ 全角カナ
  - 全角英数字 ↔ 半角英数字
- 半角スペース区切りで AND 検索（複数語の絞り込み）。
- 検索欄右端に **クリアボタン (×)** を設け、入力を一括でクリアできる。
- 物品はカード形式で一覧表示し、材料名・製品記号・棚番号・通称メモを視認できる。
- **すでにカートに入っている物品** は視覚的に強調する：
  - カードの枠線を強調（`border-ink` + リング）し、左端にアクセントバーを表示
  - 右上の「+ 追加」バッジを「✓ カート N」（黒地白文字）に置換し、現在のカート個数を表示
  - 数量入力ポップアップでも「現在カートに N 個（上書き）」のヒントを表示。入力欄には現在の個数がプリセットされ、確定時はカートの数量を **入力値で上書き** する（加算しない）。確定ボタンの文言は「カートに追加」→「カートを更新」に切替わる。
  - 数量入力ポップアップに **「カートから削除」ボタン**（ゴミ箱アイコン付き）を設け、ワンクリックで対象物品をカートから外せる。検索カード自体には削除 UI を置かない（誤操作回避）。

### 5.2 カート / 依頼フロー（画面 2 = モーダル）

- 物品カードをクリックすると **数量入力ポップアップ** が自動表示される。
  - 数量は **半角数字のみ** を受け付ける（IME 入力された全角数字や記号は自動除去）。
  - 入力欄はフォーカス／クリック時に内容を全選択。入力欄右端の × ボタンで一括クリアできる。
  - Enter キーで確定、Esc キーでキャンセル。
- 数量を確定するとカートに追加される。**すでにカートに入っている物品を再度選択した場合は、入力値で個数を上書き** する（加算しない）。
- すでにカートに入っている物品の場合、ポップアップ内に「**カートから削除**」ボタン（ゴミ箱アイコン付き）を表示し、ワンクリックでカートから外すことができる（= その物品を「未選択」状態に戻す）。
- カートは **画面 1 上のモーダル** として開く。開く起点は画面右下の **フロートカートアイコン**（`/status*` 以外で常時表示。バッジで件数を表示）。
- カートモーダルの操作：
  - 追加済み物品の **数量を修正**（± ボタン または直接入力。半角数字のみ）。数量入力欄はフォーカス／クリック時に内容を全選択し、そのままタイプして上書きできる。
  - 数量入力欄を空にして確定（フォーカス外し or Enter）した場合、または `−` ボタンで 0 に到達した場合は、その物品を **カートから削除** する。
  - 追加済み物品の **削除**
  - 「すべて削除」でカートクリア
  - 「配送依頼を送信」で依頼を確定
  - 「続けて検索」または × / Esc / 背景クリックで閉じる
- 送信成功時はモーダル内で **「依頼を送信しました」** の完了表示に切り替わる。「続けて依頼する」で閉じる。
- カートの内容は `sessionStorage` に保持し、ブラウザを開いている間はリロード後も維持される。

### 5.3 手術室の識別

- 依頼時に **手術室番号をプルダウン**（OP1〜OP10）で選択する。
- 初回アクセス時に選択を促すモーダルでブロックする。
- 選択した部屋番号は `sessionStorage` に保存され、当該ブラウザセッション中は再選択不要。
- ヘッダ右上のセレクタからいつでも切替可能。
- 依頼内容にどの手術室からの依頼かが明示される。

### 5.4 通知・アラーム（画面 3）

- サプライ課側の `/status` ページは Server-Sent Events で `/api/stream` に接続。
- 新規依頼が作成されると `new` イベントで一覧に即時反映され、画面上部に新着バナーを 5 秒間表示。
- 同時に WebAudio による **3 音シーケンスのアラーム** を鳴らす。
- ブラウザの自動再生ポリシーを満たすため、初回のみ画面上の「通知音 ON」ボタンを手動で 1 度クリックする必要がある。
- **通知音 ON/OFF の状態は `localStorage` に永続化** され、タブ遷移／再読み込み／ブラウザを閉じても維持される。リロード後は次に画面のどこかをクリック／キー押下した時点で AudioContext が自動再開され、サプライ課スタッフは再度ボタンを押す必要がない。
- 接続状態は「接続中 / 再接続中…」で常時表示。

### 5.5 依頼状況管理（画面 3 / 画面 4）

- 依頼一覧は **カード形式** で表示し、新しい順に並べる。
- カードは「未完了」「完了済み」の 2 セクションに分類。
- 完了状態は明確に **グレーアウト表示**（背景・枠線・テキスト色を抑える）。
- 完了は以下の 2 箇所から行える：
  - 受付状況のカード上の「完了にする」ボタン
  - 依頼詳細画面の「配送完了にする」ボタン

## 6. UI / デザイン方針

- 事務用途のため、**シンプルで視認性の高い構成** を優先する。
- アイコンは **モノクローム系のシンプルなもの**（lucide-react）を使用。色付きアイコンは使わない。
- 配色はモノトーン基調（白・黒・グレー）。状態表現には濃淡で差をつける。
- フォーム要素は十分なヒット領域を確保（タッチ操作にも耐える）。

## 7. データモデル

### 7.1 物品マスタ

```ts
type Item = {
  code: number;     // 物品コード
  name: string;     // 材料名（正式名称）
  spec: string;     // 製品記号
  shelf: string;    // 棚番号
  memo: string;     // メモ（通称・検索しやすいワード）
};
```

> 在庫管理は本システムの対象外のため `現在庫数` は保持しない。

### 7.2 依頼

```ts
type CartLine = {
  itemCode: number;
  quantity: number;
};

type Order = {
  id: string;                       // UUID
  room: string;                     // OP1..OP10
  lines: CartLine[];
  createdAt: string;                // ISO8601
  status: "pending" | "completed";
  completedAt?: string;
};
```

## 8. API

| メソッド | パス | 役割 |
|------|------|------|
| GET | `/api/items` | 物品マスタ取得 |
| GET | `/api/orders` | 依頼一覧（新しい順） |
| POST | `/api/orders` | 依頼作成（`{ room, lines }`） |
| GET | `/api/orders/{id}` | 依頼詳細 |
| PATCH | `/api/orders/{id}` | 依頼ステータス更新（`{ status: "completed" }`） |
| GET | `/api/stream` | SSE（`hello` / `new` / `update` / `ping`） |

## 9. 主要ファイル

| ファイル | 役割 |
|------|------|
| `app/page.tsx` | 物品検索画面 |
| `app/status/page.tsx` | 受付状況一覧 |
| `app/status/[id]/page.tsx` | 依頼詳細 |
| `app/api/**` | API ルート |
| `components/CartModal.tsx` | カート / 配送依頼モーダル |
| `components/FloatingCartButton.tsx` | 右下フロート FAB |
| `components/Header.tsx` | グローバルヘッダ |
| `components/QuantityDialog.tsx` | 数量入力ポップアップ |
| `components/RoomGate.tsx` | 手術室未選択時のブロックモーダル |
| `components/SearchClient.tsx` | 物品検索 UI |
| `components/StatusClient.tsx` | 受付状況 UI（SSE 接続） |
| `components/DetailClient.tsx` | 依頼詳細 UI |
| `components/providers.tsx` | `RoomContext` / `CartContext`（モーダル開閉状態を含む） |
| `lib/db.ts` | インメモリ依頼ストア |
| `lib/items.ts` | 物品マスタ種データ |
| `lib/normalize.ts` | 検索文字列正規化（NFKC + lowercase + ひら→カナ） |
| `lib/beep.ts` | WebAudio アラーム |

## 10. 補足・検討事項

- データベース選定（院内情報の取り扱い、Vercel Postgres / Neon / 院内DB 等）。
  - 現時点では **共有サーバ上の Excel ファイル** を使う方向で検討中。決定したら `lib/db.ts` の `OrderStore` インターフェイスをそのまま実装差し替えで対応する。
  - Excel 採用時の留意点:
    - **物品マスタ**（読み取り中心、業務側でメンテ）は Excel と相性が良い。
    - **依頼データ**（書き込み頻度高、複数手術室から同時書き込み）は Excel が苦手。誰かが Excel を開いていると書き込み失敗、複数同時更新で行が飛ぶリスク。
    - 対策案: (a) 物品マスタのみ Excel、依頼データは別形式（SQLite/JSON など）  (b) サーバ側でキューイングして書き込みを直列化  (c) 書き込みは内部 DB に行い定期的に Excel へエクスポート。
    - ファイル I/O は `exceljs` / `xlsx` などのライブラリを利用。Vercel デプロイ時は共有サーバへネットワーク越しにアクセスする必要があるため、デプロイ先の見直し（院内サーバへ Self-Host など）も含めて検討。
- 通知強化（OS Push、メール、デスクトップ通知）。
- ユーザ認証 / ロール分離（手術室側 / サプライ課側）。
- 物品マスタの管理 UI（追加・編集・廃止）。

## 11. 変更履歴

| 日付 | 変更内容 |
|------|------|
| 2026-05-06 | 初版作成 |
| 2026-05-06 | 画面 2「カート / 配送依頼」を独立ページから検索画面上のモーダルに変更。右下フロートカートアイコンとヘッダ「カート」ボタンの双方からモーダルを開く仕様を追加。 |
| 2026-05-06 | ヘッダのカートタブを廃止。カートを開く起点はフロート FAB のみに統一。 |
| 2026-05-06 | アプリ名を「サプリンク」、サブタイトルを「物品オーダーシステム」に決定。 |
| 2026-05-06 | 検索結果カードで、すでにカートに入っている物品を強調表示する仕様を追加。 |
| 2026-05-06 | カート内物品を再選択した際の挙動を「加算」から「上書き」に変更。数量入力ポップアップの初期値を現在のカート個数とし、確定ボタンを「カートを更新」に切替。 |
| 2026-05-06 | カートモーダル内の数量入力欄をフォーカス／クリック時に全選択する挙動に変更（クリック→上書き入力をスムーズに）。 |
| 2026-05-06 | 物品選択時の数量入力ポップアップに、入力欄右端の × クリアボタンを追加。同入力欄も focus/click で全選択。 |
| 2026-05-06 | 数量を 0 または空で確定した場合、対象物品をカートから削除する仕様に変更（= 未選択状態へ戻す）。`setQuantity` を upsert/remove に拡張し、QuantityDialog・CartModal 双方に反映。 |
| 2026-05-06 | 物品検索カード上に、カート在中時のみ表示される「× カートから削除」ボタンを追加。ポップアップを開かずに削除可能。 |
| 2026-05-06 | 検索カードからの削除ボタンを取り下げ、数量入力ポップアップ側に専用の「カートから削除」ボタンを実装する方針に変更（誤操作回避）。 |
| 2026-05-06 | 通知音 ON/OFF の状態を `localStorage` に永続化。リロード後も状態を保持し、最初のユーザ操作で AudioContext を自動再開する。 |
| 2026-05-06 | データベースの方向性を「共有サーバ上の Excel ファイル」候補として記載（未確定）。Excel 採用時の留意点（同時書き込み、ファイルロック、運用パターン）を補足。 |
